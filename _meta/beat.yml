################### Safecastbeat Configuration Example #########################

############################# Safecastbeat ######################################

safecastbeat:
  # Defines how often an event is sent to the output and also
  # calculates the period of data pull from safecast
  period: 5m

  # Defines SSL configuration.
  ssl:
    cafile: "/path/to/CA.pem"
